{
  "mcpServers": {
    "ai-crew-sync": {
      "type": "http",
      "url": "${BUS_URL}",
      "headers": {
        "Authorization": "Bearer ${BUS_TOKEN}",
        "X-Crew-Session": "${BUS_SESSION:-}"
      }
    }
  }
}
